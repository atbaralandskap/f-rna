# AGENTS.md

## Project Rules
- Never delete `docs/CNAME`.
- Preserve `docs/CNAME` value as `xn--frna-5qa.se` unless explicitly instructed to change the custom domain.
- After any `quarto render`, verify `docs/CNAME` still exists before committing.
